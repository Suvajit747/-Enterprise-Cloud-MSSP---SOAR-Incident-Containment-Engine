# Base URL of the SOAR Incident Containment Engine FastAPI backend.
# This is the ONLY backend configuration this frontend needs.
# Never put API keys (VirusTotal, AbuseIPDB) or database credentials here —
# anything prefixed VITE_ is bundled into public, client-side JavaScript.
VITE_API_BASE_URL=http://127.0.0.1:8000
